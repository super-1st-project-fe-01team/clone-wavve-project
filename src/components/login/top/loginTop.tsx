import React from "react";
import styles from "./loginTop.module.css";

const loginTop = () => {
  return (
    <div className={styles.loginTop}>
      <h1>로그인</h1>
    </div>
  );
};

export default loginTop;
